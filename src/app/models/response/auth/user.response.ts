export interface RegisterResponse {
  // user: User;
  // access_token: string;
  // refreshToken?: string;
  name: string;
}
export interface LoginResponse {
  name: string;
  email: string;
  uniqueId: string;
}
